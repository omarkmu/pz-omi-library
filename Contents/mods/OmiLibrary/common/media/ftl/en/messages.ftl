### Messages used by the library

## Form messages

form-default =
    .title = Configuration
    .save-success = Saved successfully.
    .save-failure = Failed to save.
    .action-default = Action

form-empty = (empty)

form-compatibility =
    .option-Enable = Enable
    .option-Disable = Disable
    .option-Auto = Enable if mod is enabled

## Gamepad prompts

prompt-edit = Edit

prompt-color-picker = Open Color Picker
